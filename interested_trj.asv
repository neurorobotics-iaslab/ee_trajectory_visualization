function ee = interested_trj(event, ee, event_req)
    c_ee.tr.x = [];
    c_ee.tr.y = [];
    c_ee.tr.w = [];

    % extract only interested events
    POS = event.POS(event.POS(ismember(event.TYP, event_req))~=0);
    for i=1:numel(event.POS)
        
    end
end